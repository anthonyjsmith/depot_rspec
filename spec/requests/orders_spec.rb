require 'spec_helper'

describe "Orders" do
  describe "GET /orders" do
    it "works! (now write some real specs)" do
      get orders_path
      expect(response.status).to be(200)
    end
  end
end
